# Cookie validation utilities.


def validate_cookie(cookie_id: str) -> dict | None:
    """Return the user record for cookie_id, or None if invalid/expired."""
    # TODO: query cookie table, check expire_time, return user
    raise NotImplementedError("auth.validate_cookie not yet implemented")
